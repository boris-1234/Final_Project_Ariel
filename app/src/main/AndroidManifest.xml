<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.final_project_ariel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Final_Project_Ariel">

        <activity android:name=".PagesPackage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PagesPackage.HCTenantPaymentActivity" />
        <activity android:name=".PagesPackage.HouseCommitteeActivity" />
        <activity android:name=".PagesPackage.TenantActivity" />
        <activity android:name=".PagesPackage.HCTotalPricesInTheBuildingActivity" />
        <activity android:name=".PagesPackage.HCTotalPayments" />
        <activity android:name=".PagesPackage.HCUpdatePaymentTenant" />
    </application>

</manifest>